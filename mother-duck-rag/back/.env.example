# Caminho para seu vault Obsidian (pasta com arquivos .md)
VAULT_PATH=/test_vault

# Arquivo DuckDB local (banco com notas, chunks, embeddings)
DB_PATH=test_brain.duckdb

# Modelo de embeddings (escolha um):
# - all-MiniLM-L6-v2: 384 dims, rápido, bom para testar
# - BAAI/bge-m3: 1024 dims, melhor qualidade, ~1.8GB
MODEL=all-MiniLM-L6-v2

# Chat / Q&A (optional): LLM for answering over your vault
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1   # or Ollama: http://localhost:11434/v1
# OPENAI_MODEL=gpt-4o-mini

# Web research (optional): Perplexity Sonar for web-grounded answers (agent tool + Research tab)
# PERPLEXITY_API_KEY=pplx-...
# PERPLEXITY_BASE_URL=https://api.perplexity.ai   # Sonar API
# PERPLEXITY_MODEL=sonar   # or sonar-pro for more citations
